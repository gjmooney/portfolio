export interface ExperienceText {
  title: string;
  company: string;
  companyLink: string;
  dates: string;
  location: string;
  points: string[];
}

export interface EducationText {
  title: string;
  school: string;
  dates: string;
  location: string;
  info: string;
}
